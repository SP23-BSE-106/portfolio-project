# TODO: Fix TypeScript Errors

- [x] Update fetch mock in `src/app/api/contact/route.test.ts` to return a proper Response-like object
- [x] Remove duplicate React import in `src/app/components/sections/Contact.test.tsx`
- [x] Create type declaration file for 'react-google-recaptcha' since @types package doesn't exist
- [x] Run `npx tsc --noEmit` to verify all errors are fixed
